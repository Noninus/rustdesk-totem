name: Build Android APK (Sagres Totem)

on:
  workflow_dispatch:

jobs:
  build-android:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "totem"
